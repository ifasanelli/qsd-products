<h1 class="ls-title-intro ls-ico-edit-admin">
  <%= Price.model_name.human(count: 2) %>
</h1>
<strong><p class="ls-alert-danger"><%= alert %></p></strong>
<strong><p class="ls-alert-success"><%= notice %></p></strong>

<% if @prices.empty? %>
  Lista Vazia
<% end %>
<div class="ls-box">
  <header class="ls-info-header">
    <h6 class="ls-title-4">Lista de Preços</h6>
  </header>
  <% @prices.each do |price| %>
    <div class="ls-list">
      <header class="ls-list-header">
        <div class="ls-list-title col-md-9">
          <%= link_to format('R$ %.2f', price.plan_price), edit_price_path(price) %>
        </div>
        <div class="col-md-3 ls-txt-right">
          <%= link_to 'Alterar', edit_price_path(price), class:"ls-btn-primary" %>
        </div>
      </header>
      <div class="ls-list-content ">
        <div class="col-xs-12 col-md-6">
          <span class="ls-list-label"><%= Plan.model_name.human %></span>
          <strong><%= link_to price.plan.name, plans_path %></strong>
        </div>
      </div>
     <div class="ls-list-content ">
        <div class="col-xs-12 col-md-6">
          <span class="ls-list-label"><%= Periodicity.model_name.human %></span>
          <strong><%= link_to price.periodicity.name, periodicities_path %></strong>
        </div>
      </div>
    </div>
  <% end %>
</div>
<h3> Cadastrar Preço </h3><br \>
<div class="ls-box ls-box-gray">
  <%= render partial: 'form', locals: { price: @price } %>
</div>
