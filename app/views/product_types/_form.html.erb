<% if @product_type.errors.any? %>
  Você deve corrigir os seguintes erros:
  <% @product_type.errors.full_messages.each do |msg| %>
    <p> <%= msg %> </p>
  <% end %>
<% end %>
<%= form_with model: @product_type, class:"ls-form ls-form-inline row", local: true do |f| %>
  <label class="ls-label col-md-5">
    <b class="ls-label-text"><%= f.label :name %></b>
    <%= f.text_field :name %>
  </label>
  <label class="ls-label col-md-4">
    <b class="ls-label-text"><%= f.label :description %></b>
    <%= f.text_area :description %>
  </label>
  <label class="ls-label col-md-4">
    <b class="ls-label-text"><%= f.label :product_key %></b>
    <%= f.text_area :product_key %>
  </label>
  <div class="ls-actions-btn">
  <%= f.submit class: 'ls-btn'%>
  </div>
<% end %>
